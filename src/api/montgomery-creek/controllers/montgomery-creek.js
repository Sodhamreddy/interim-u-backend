'use strict';

/**
 * montgomery-creek controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::montgomery-creek.montgomery-creek');
